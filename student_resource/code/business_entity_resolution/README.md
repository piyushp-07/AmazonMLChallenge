# Member 2: Matching & Scoring

This package implements Member 2's matching and scoring pipeline. It consumes `output/candidate_pairs.tsv` and produces `output/matching_results.tsv`.

Run the pipeline from `student_resource/code/business_entity_resolution/`.

Commands:

python3 -m src.pipeline
python3 -m src.inference
